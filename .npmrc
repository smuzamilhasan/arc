auto-install-peers=false
strict-peer-dependencies=false
# The service sets NODE_ENV=production, so Nixpacks' first install is prod-only.
# The build then re-installs with --prod=false to get devDependencies (vite,
# esbuild, etc.); this lets pnpm purge+reinstall without an interactive prompt.
confirm-modules-purge=false
