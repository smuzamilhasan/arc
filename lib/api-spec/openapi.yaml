openapi: 3.1.0
info:
  # Do not change the title, if the title changes, the import paths will be broken
  title: Api
  version: 0.1.0
  description: arc — Personal Brand Strategy API
servers:
  - url: /api
    description: Base API path
tags:
  - name: health
    description: Health operations
  - name: client
    description: Client profile operations
  - name: audit
    description: Digital presence audit operations
  - name: narrative
    description: Narrative development operations
  - name: portrait
    description: AI-synthesized foundational profile portrait
  - name: posts
    description: Content post operations
  - name: ideas
    description: Content idea operations
  - name: dashboard
    description: Dashboard summary operations
  - name: onboarding
    description: Onboarding intake operations
  - name: admin
    description: Admin operations (view all users and their data)
  - name: assistant
    description: Master assistant chat that proposes confirm-before-apply edits
  - name: planner
    description: Planner agent — turns the approved strategy into a weekly content calendar and ideas backlog
  - name: manager
    description: Manager — orchestrates the agent team from one high-level instruction
paths:
  /healthz:
    get:
      operationId: healthCheck
      tags: [health]
      summary: Health check
      responses:
        "200":
          description: Healthy
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/HealthStatus"

  /client:
    get:
      operationId: getClient
      tags: [client]
      summary: Get client profile
      responses:
        "200":
          description: Client profile
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ClientProfile"
        "404":
          description: Not found
    put:
      operationId: upsertClient
      tags: [client]
      summary: Create or update client profile
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/ClientProfileInput"
      responses:
        "200":
          description: Updated client profile
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ClientProfile"

  /client/reset:
    post:
      operationId: resetClient
      tags: [client]
      summary: Delete the client profile and all derived data (start over)
      description: Permanently removes the client profile along with its audit results, narrative, posts, and ideas, returning the app to a brand-new state.
      responses:
        "204":
          description: Reset complete

  /client/foundation-ack:
    post:
      operationId: ackFoundationConsolidation
      tags: [client]
      summary: Acknowledge the one-time foundation consolidation celebration
      description: Sets foundationConsolidatedAck to true so the consolidation celebration modal never shows again.
      responses:
        "200":
          description: Updated client profile
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ClientProfile"

  /account:
    delete:
      operationId: deleteAccount
      tags: [client]
      summary: Permanently delete the user's account and all associated data
      description: Deletes the client profile along with its audit results, narrative, posts, and ideas, then removes the user's authentication account. This cannot be undone.
      responses:
        "204":
          description: Account deleted

  /audit/latest:
    get:
      operationId: getLatestAudit
      tags: [audit]
      summary: Get the most recent audit result
      responses:
        "200":
          description: Latest audit result
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AuditResult"
        "404":
          description: No audit yet

  /audit/run:
    post:
      operationId: runAudit
      tags: [audit]
      summary: Run a digital presence audit (SSE stream)
      description: Streams progress events via Server-Sent Events, then persists and returns the result. Consume with fetch + ReadableStream, not generated hooks.
      requestBody:
        required: false
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/AuditRunInput"
      responses:
        "200":
          description: SSE stream of audit progress
          content:
            text/event-stream:
              schema:
                type: string

  /audit/refresh-status:
    get:
      operationId: getAuditRefreshStatus
      tags: [audit]
      summary: Whether a background audit refresh is currently running for the user
      responses:
        "200":
          description: Refresh status
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AuditRefreshStatus"

  /audit/auto-refresh:
    post:
      operationId: autoRefreshAudit
      tags: [audit]
      summary: Start a background audit refresh if the latest audit is stale
      description: Triggered on sign-in. If the client already has at least one audit that is 14+ days old and no audit is currently running, a fresh audit starts in the background and the response indicates it was started. Otherwise nothing is started. Never starts the first-ever audit.
      responses:
        "200":
          description: Whether a refresh was started
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AuditAutoRefreshResult"

  /dossier:
    get:
      operationId: getDossier
      tags: [dossier]
      summary: Get the latest briefing dossier
      responses:
        "200":
          description: Briefing dossier
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BriefingDossier"
        "404":
          description: Not found

  /dossier/generate:
    post:
      operationId: generateDossier
      tags: [dossier]
      summary: Research the client and competitors into a briefing dossier
      requestBody:
        required: false
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/DossierGenerateInput"
      responses:
        "200":
          description: Generated briefing dossier
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BriefingDossier"
        "404":
          description: No client profile yet
        "502":
          description: Generation failed

  /narrative:
    get:
      operationId: getNarrative
      tags: [narrative]
      summary: Get the narrative profile
      responses:
        "200":
          description: Narrative profile
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/NarrativeProfile"
        "404":
          description: Not found
    put:
      operationId: updateNarrative
      tags: [narrative]
      summary: Update the editable fields of the narrative profile
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/NarrativeUpdateInput"
      responses:
        "200":
          description: Updated narrative profile
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/NarrativeProfile"
        "404":
          description: Not found

  /narrative/generate:
    post:
      operationId: generateNarrative
      tags: [narrative]
      summary: Generate a narrative from industry questionnaire answers
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/NarrativeGenerateInput"
      responses:
        "200":
          description: Generated narrative profile
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/NarrativeProfile"

  /portrait:
    get:
      operationId: getPortrait
      tags: [portrait]
      summary: Get the synthesized foundational profile portrait
      responses:
        "200":
          description: Profile portrait
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ProfilePortrait"
        "404":
          description: Not found

  /portrait/generate:
    post:
      operationId: generatePortrait
      tags: [portrait]
      summary: Generate (or regenerate) the foundational profile portrait from the current profile
      requestBody:
        required: false
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/PortraitGenerateInput"
      responses:
        "200":
          description: Generated profile portrait
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ProfilePortrait"
        "404":
          description: Not found

  /platforms:
    get:
      operationId: getPlatforms
      tags: [platforms]
      summary: Get the platform strategy
      responses:
        "200":
          description: Platform strategy
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PlatformStrategy"
        "404":
          description: Not found
    put:
      operationId: updatePlatforms
      tags: [platforms]
      summary: Update the editable text fields of the platform strategy
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/PlatformsUpdateInput"
      responses:
        "200":
          description: Updated platform strategy
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PlatformStrategy"
        "404":
          description: Not found

  /platforms/generate:
    post:
      operationId: generatePlatforms
      tags: [platforms]
      summary: Generate a platform strategy from the completed blueprint
      requestBody:
        required: false
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/PlatformsGenerateInput"
      responses:
        "200":
          description: Generated platform strategy
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PlatformStrategy"
        "403":
          description: Blueprint incomplete
        "404":
          description: No client profile yet
        "502":
          description: Generation failed

  /industry-overview:
    get:
      operationId: getIndustryOverview
      tags: [industryOverview]
      summary: Get the latest industry overview
      responses:
        "200":
          description: Industry overview
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/IndustryOverview"
        "404":
          description: Not found

  /industry-overview/generate:
    post:
      operationId: generateIndustryOverview
      tags: [industryOverview]
      summary: Research the client's industry landscape into an industry overview
      requestBody:
        required: false
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/IndustryOverviewGenerateInput"
      responses:
        "200":
          description: Generated industry overview
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/IndustryOverview"
        "403":
          description: Foundation incomplete
        "404":
          description: No client profile yet
        "502":
          description: Generation failed

  /content-strategy:
    get:
      operationId: getContentStrategy
      tags: [contentStrategy]
      summary: Get the content strategy
      responses:
        "200":
          description: Content strategy
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ContentStrategy"
        "404":
          description: Not found
    put:
      operationId: updateContentStrategy
      tags: [contentStrategy]
      summary: Update the editable text fields of the content strategy
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/ContentStrategyUpdateInput"
      responses:
        "200":
          description: Updated content strategy
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ContentStrategy"
        "404":
          description: Not found

  /content-strategy/generate:
    post:
      operationId: generateContentStrategy
      tags: [contentStrategy]
      summary: Generate a content strategy from the blueprint and platform strategy
      requestBody:
        required: false
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/ContentStrategyGenerateInput"
      responses:
        "200":
          description: Generated content strategy
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ContentStrategy"
        "403":
          description: Blueprint or platform strategy incomplete
        "404":
          description: No client profile yet
        "502":
          description: Generation failed

  /planner/generate:
    post:
      operationId: generateContentPlan
      tags: [planner]
      summary: Propose a weekly content calendar from the approved strategy
      description: Reads the narrative, content strategy, and platform strategy and proposes a calendar of content slots plus new backlog ideas. Nothing is persisted — the client confirms via /planner/apply.
      requestBody:
        required: false
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/ContentPlanGenerateInput"
      responses:
        "200":
          description: Proposed content plan
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ContentPlanProposal"
        "403":
          description: Blueprint or content strategy incomplete
        "404":
          description: No client profile yet
        "502":
          description: Planning failed

  /planner/apply:
    post:
      operationId: applyContentPlan
      tags: [planner]
      summary: Commit a confirmed content plan into scheduled posts and backlog ideas
      description: Persists the confirmed slots as scheduled draft posts and the confirmed ideas into the backlog. Human-in-the-loop — only called after the client reviews the proposal.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/ContentPlanApplyInput"
      responses:
        "200":
          description: The created posts and ideas
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ContentPlanApplyResult"
        "404":
          description: No client profile yet

  /manager/run:
    post:
      operationId: runManager
      tags: [manager]
      summary: Orchestrate the agent team from one high-level instruction
      description: Breaks a single client instruction into an ordered set of delegated tasks, routes each to the right specialist agent (Investigator, Strategist, Planner, Ghostwriter) in research -> strategy -> planning -> writing order, runs them so each reads the latest upstream output, and returns the run with each task's status and outputs. Bounded — at most one task per agent. Outputs that change anything stay un-applied and are confirmed by the client through the existing per-agent surfaces.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/ManagerRunInput"
      responses:
        "200":
          description: The completed Manager run
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ManagerRun"
        "400":
          description: Missing or invalid instruction
        "404":
          description: No client profile yet
        "429":
          description: Rate limited or a run is already in flight
        "502":
          description: The Manager could not break down the instruction

  /manager/runs:
    get:
      operationId: listManagerRuns
      tags: [manager]
      summary: List past Manager runs for the client, newest first
      responses:
        "200":
          description: Manager runs
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/ManagerRun"
        "404":
          description: No client profile yet

  /onboarding/extract:
    post:
      operationId: extractPublicInfo
      tags: [onboarding]
      summary: Gather publicly available info about the person from their name and links
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/ExtractInfoInput"
      responses:
        "200":
          description: Extracted public info
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ExtractInfoResult"

  /onboarding/generate-bio:
    post:
      operationId: generateBio
      tags: [onboarding]
      summary: Generate a professional headline and short bio from intake material
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/GenerateBioInput"
      responses:
        "200":
          description: Generated headline and bio
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/GenerateBioResult"

  /onboarding/draft-pillar:
    post:
      operationId: draftPillar
      tags: [onboarding]
      summary: Draft tentative answers for a Blueprint pillar from existing intake material
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/DraftPillarInput"
      responses:
        "200":
          description: Drafted field values keyed by field name
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/DraftPillarResult"

  /onboarding/pillar-examples:
    post:
      operationId: generatePillarExamples
      tags: [onboarding]
      summary: Generate industry-tailored illustrative example answers for a Blueprint pillar's fields
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/GeneratePillarExamplesInput"
      responses:
        "200":
          description: Illustrative example strings keyed by field name
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/GeneratePillarExamplesResult"

  /posts:
    get:
      operationId: listPosts
      tags: [posts]
      summary: List all posts
      parameters:
        - name: platform
          in: query
          schema:
            type: string
        - name: status
          in: query
          schema:
            type: string
      responses:
        "200":
          description: List of posts
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/Post"
    post:
      operationId: createPost
      tags: [posts]
      summary: Create a post
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/PostInput"
      responses:
        "201":
          description: Created post
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Post"

  /posts/schedule-batch:
    post:
      operationId: scheduleBatchPosts
      tags: [posts]
      summary: Schedule a batch of posts across dates
      description: Assigns target dates to several posts at once, spreading them from a start date by a fixed day interval, and marks each as scheduled.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/ScheduleBatchInput"
      responses:
        "200":
          description: The updated posts after scheduling
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/Post"
        "400":
          description: Invalid input
        "404":
          description: No client profile yet

  /posts/draft:
    post:
      operationId: draftPosts
      tags: [posts]
      summary: Draft platform-appropriate copy in the client's voice (Ghostwriter)
      description: Generates one or more editable content drafts (post, hook, or article) grounded in the client's narrative, voice/tone, and profile. Drafts are returned for review and are NOT persisted; the client edits and saves the ones they want via the normal create-post route.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/DraftPostsInput"
      responses:
        "200":
          description: Generated drafts for review
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/DraftPostsResult"
        "400":
          description: Invalid input
        "404":
          description: No client profile yet
        "502":
          description: Generation failed

  /posts/{id}:
    get:
      operationId: getPost
      tags: [posts]
      summary: Get a post by ID
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
      responses:
        "200":
          description: Post
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Post"
        "404":
          description: Not found
    patch:
      operationId: updatePost
      tags: [posts]
      summary: Update a post
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/PostUpdate"
      responses:
        "200":
          description: Updated post
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Post"
        "404":
          description: Not found
    delete:
      operationId: deletePost
      tags: [posts]
      summary: Delete a post
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
      responses:
        "204":
          description: Deleted
        "404":
          description: Not found

  /ideas:
    get:
      operationId: listIdeas
      tags: [ideas]
      summary: List content ideas
      responses:
        "200":
          description: List of ideas
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/Idea"
    post:
      operationId: createIdea
      tags: [ideas]
      summary: Create a content idea
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/IdeaInput"
      responses:
        "201":
          description: Created idea
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Idea"

  /ideas/{id}:
    delete:
      operationId: deleteIdea
      tags: [ideas]
      summary: Delete a content idea
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
      responses:
        "204":
          description: Deleted
        "404":
          description: Not found
    patch:
      operationId: updateIdea
      tags: [ideas]
      summary: Update a content idea
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/IdeaUpdate"
      responses:
        "200":
          description: Updated idea
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Idea"

  /dashboard:
    get:
      operationId: getDashboard
      tags: [dashboard]
      summary: Get dashboard summary
      responses:
        "200":
          description: Dashboard summary
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/DashboardSummary"

  /admin/access:
    get:
      operationId: getAdminAccess
      tags: [admin]
      summary: Whether the current user has admin access
      responses:
        "200":
          description: Admin access flag
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AdminAccess"

  /admin/users:
    get:
      operationId: listAdminUsers
      tags: [admin]
      summary: List all users with a summary of their data (admin only)
      responses:
        "200":
          description: All users
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/AdminUserSummary"
        "403":
          description: Forbidden

  /admin/users/{clientId}:
    get:
      operationId: getAdminUser
      tags: [admin]
      summary: Get a single user's full profile, narrative, audit, posts, and ideas (admin only)
      parameters:
        - name: clientId
          in: path
          required: true
          schema:
            type: integer
      responses:
        "200":
          description: User detail
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AdminUserDetail"
        "403":
          description: Forbidden
        "404":
          description: Not found

  /assistant/messages:
    get:
      operationId: getAssistantMessages
      tags: [assistant]
      summary: Get the persisted assistant conversation for the client
      responses:
        "200":
          description: Conversation messages in chronological order
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/AssistantMessage"
        "404":
          description: No client profile yet

  /assistant/unread:
    get:
      operationId: getAssistantUnread
      tags: [assistant]
      summary: Count unseen proactive assistant messages for the client
      responses:
        "200":
          description: Number of unseen messages
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AssistantUnread"
        "404":
          description: No client profile yet

  /assistant/seen:
    post:
      operationId: markAssistantSeen
      tags: [assistant]
      summary: Mark all assistant messages for the client as seen
      responses:
        "200":
          description: Remaining unseen count (always zero)
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AssistantUnread"
        "404":
          description: No client profile yet

  /assistant/stream:
    get:
      operationId: streamAssistant
      tags: [assistant]
      summary: Live notifications of new proactive assistant messages (SSE stream)
      description: Streams Server-Sent Events when the background strategist posts a new proactive suggestion. Consume with fetch + ReadableStream, not generated hooks.
      responses:
        "200":
          description: SSE stream of assistant notifications
          content:
            text/event-stream:
              schema:
                type: string

  /assistant/message:
    post:
      operationId: sendAssistantMessage
      tags: [assistant]
      summary: Send a message and get an assistant reply with proposed actions
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/AssistantSendInput"
      responses:
        "200":
          description: The persisted user message and the assistant reply
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AssistantReply"
        "400":
          description: Invalid input
        "404":
          description: No client profile yet
        "502":
          description: Assistant generation failed

  /assistant/actions/{actionId}/confirm:
    post:
      operationId: confirmAssistantAction
      tags: [assistant]
      summary: Apply a proposed action to the underlying system
      parameters:
        - name: actionId
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: The applied action and an optional follow-up message
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AssistantActionResult"
        "400":
          description: Action could not be applied
        "404":
          description: Action not found

  /assistant/actions/{actionId}/reject:
    post:
      operationId: rejectAssistantAction
      tags: [assistant]
      summary: Reject a proposed action, optionally with a comment for revision
      parameters:
        - name: actionId
          in: path
          required: true
          schema:
            type: string
      requestBody:
        required: false
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/AssistantRejectInput"
      responses:
        "200":
          description: The rejected action and the assistant's revised reply
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AssistantActionResult"
        "404":
          description: Action not found
        "502":
          description: Assistant generation failed

  /assistant/actions/confirm-batch:
    post:
      operationId: confirmAssistantActions
      tags: [assistant]
      summary: Apply several proposed actions at once
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/AssistantBatchInput"
      responses:
        "200":
          description: The updated actions after applying
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AssistantBatchResult"
        "400":
          description: Invalid input
        "404":
          description: No client profile yet

  /assistant/actions/reject-batch:
    post:
      operationId: rejectAssistantActions
      tags: [assistant]
      summary: Reject several proposed actions at once
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/AssistantBatchInput"
      responses:
        "200":
          description: The updated actions after rejecting
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AssistantBatchResult"
        "400":
          description: Invalid input
        "404":
          description: No client profile yet

components:
  schemas:
    HealthStatus:
      type: object
      properties:
        status:
          type: string
      required:
        - status

    ClientProfile:
      type: object
      required: [id, fullName, location, headline, currentRole, company, industry, yearsExperience, achievements, goals, bio, placeOfBirth, earlyLife, schooling, university, professionalJourney, signatureAchievements, awards, quantifiableResults, audienceImpact, passions, beliefs, frustrations, desiredChange, positioning, primaryAudience, secondaryAudience, geographyCulture, brandValues, nonNegotiables, personalityTone, desiredFeeling, thesis, coreBeliefs, signatureFrameworks, extractedInfo, onboardingComplete, onboardingStep, foundationConsolidatedAck, createdAt, updatedAt]
      properties:
        id:
          type: integer
        fullName:
          type: string
        location:
          type: string
        headline:
          type: string
        currentRole:
          type: string
        company:
          type: string
        industry:
          type: string
        yearsExperience:
          type: integer
        achievements:
          type: array
          items:
            type: string
        goals:
          type: string
        bio:
          type: string
        dateOfBirth:
          type: ["string", "null"]
        placeOfBirth:
          type: string
        earlyLife:
          type: string
        schooling:
          type: string
        university:
          type: string
        professionalJourney:
          type: string
        signatureAchievements:
          type: string
        awards:
          type: string
        quantifiableResults:
          type: string
        audienceImpact:
          type: string
        passions:
          type: string
        beliefs:
          type: string
        frustrations:
          type: string
        desiredChange:
          type: string
        positioning:
          type: string
        primaryAudience:
          type: string
        secondaryAudience:
          type: string
        geographyCulture:
          type: string
        brandValues:
          type: string
        nonNegotiables:
          type: string
        personalityTone:
          type: string
        desiredFeeling:
          type: string
        thesis:
          type: string
        coreBeliefs:
          type: string
        signatureFrameworks:
          type: string
        extractedInfo:
          type: string
        website:
          type: ["string", "null"]
        newsletter:
          type: ["string", "null"]
        linkedinUrl:
          type: ["string", "null"]
        twitterUrl:
          type: ["string", "null"]
        instagramUrl:
          type: ["string", "null"]
        youtubeUrl:
          type: ["string", "null"]
        onboardingComplete:
          type: boolean
        onboardingStep:
          type: integer
        foundationConsolidatedAck:
          type: boolean
        createdAt:
          type: string
        updatedAt:
          type: string

    ClientProfileInput:
      type: object
      required: [fullName]
      properties:
        fullName:
          type: string
        location:
          type: string
        headline:
          type: string
        currentRole:
          type: string
        company:
          type: string
        industry:
          type: string
        yearsExperience:
          type: integer
        achievements:
          type: array
          items:
            type: string
        goals:
          type: string
        bio:
          type: string
        dateOfBirth:
          type: string
        placeOfBirth:
          type: string
        earlyLife:
          type: string
        schooling:
          type: string
        university:
          type: string
        professionalJourney:
          type: string
        signatureAchievements:
          type: string
        awards:
          type: string
        quantifiableResults:
          type: string
        audienceImpact:
          type: string
        passions:
          type: string
        beliefs:
          type: string
        frustrations:
          type: string
        desiredChange:
          type: string
        positioning:
          type: string
        primaryAudience:
          type: string
        secondaryAudience:
          type: string
        geographyCulture:
          type: string
        brandValues:
          type: string
        nonNegotiables:
          type: string
        personalityTone:
          type: string
        desiredFeeling:
          type: string
        thesis:
          type: string
        coreBeliefs:
          type: string
        signatureFrameworks:
          type: string
        extractedInfo:
          type: string
        website:
          type: string
        newsletter:
          type: string
        linkedinUrl:
          type: string
        twitterUrl:
          type: string
        instagramUrl:
          type: string
        youtubeUrl:
          type: string
        onboardingComplete:
          type: boolean
        onboardingStep:
          type: integer
        foundationConsolidatedAck:
          type: boolean

    ExtractInfoInput:
      type: object
      required: [fullName]
      properties:
        fullName:
          type: string
        linkedinUrl:
          type: string
        website:
          type: string
        twitterUrl:
          type: string
        company:
          type: string

    ExtractedSource:
      type: object
      required: [title, url]
      properties:
        title:
          type: string
        url:
          type: string

    ExtractInfoResult:
      type: object
      required: [summary, highlights, sources]
      properties:
        summary:
          type: string
        highlights:
          type: array
          items:
            type: string
        sources:
          type: array
          items:
            $ref: "#/components/schemas/ExtractedSource"

    GenerateBioInput:
      type: object
      required: [fullName]
      properties:
        fullName:
          type: string
        currentRole:
          type: string
        company:
          type: string
        industry:
          type: string
        signatureAchievements:
          type: string
        awards:
          type: string
        quantifiableResults:
          type: string
        audienceImpact:
          type: string
        professionalJourney:
          type: string
        extractedInfo:
          type: string
        feedback:
          type: string
          description: Optional user revision notes to steer a regeneration.

    GenerateBioResult:
      type: object
      required: [headline, bio]
      properties:
        headline:
          type: string
        bio:
          type: string

    DraftPillarField:
      type: object
      required: [name, label]
      properties:
        name:
          type: string
        label:
          type: string
        multiline:
          type: boolean

    DraftPillarInput:
      type: object
      required: [pillarId, fullName, fields]
      properties:
        pillarId:
          type: string
        fullName:
          type: string
        currentRole:
          type: string
        company:
          type: string
        industry:
          type: string
        professionalJourney:
          type: string
        signatureAchievements:
          type: string
        awards:
          type: string
        quantifiableResults:
          type: string
        audienceImpact:
          type: string
        passions:
          type: string
        beliefs:
          type: string
        frustrations:
          type: string
        desiredChange:
          type: string
        thesis:
          type: string
        coreBeliefs:
          type: string
        signatureFrameworks:
          type: string
        extractedInfo:
          type: string
        feedback:
          type: string
          description: Optional user revision notes to steer a regeneration.
        fields:
          type: array
          items:
            $ref: "#/components/schemas/DraftPillarField"

    DraftPillarResult:
      type: object
      required: [fields]
      properties:
        fields:
          type: object
          additionalProperties:
            type: string

    GeneratePillarExamplesInput:
      type: object
      required: [pillarId, industry, fields]
      properties:
        pillarId:
          type: string
        industry:
          type: string
        currentRole:
          type: string
        company:
          type: string
        feedback:
          type: string
          description: Optional user revision notes to steer a regeneration.
        fields:
          type: array
          items:
            $ref: "#/components/schemas/DraftPillarField"

    GeneratePillarExamplesResult:
      type: object
      required: [fields]
      properties:
        fields:
          type: object
          additionalProperties:
            type: string

    SeoFinding:
      type: object
      required: [title, url, type, snippet]
      properties:
        title:
          type: string
        url:
          type: string
        type:
          type: string
          enum: [owned, social, press, directory, other]
        snippet:
          type: string

    SeoFindings:
      type: object
      required: [resultCount, results, ownedPresence, summary]
      properties:
        resultCount:
          type: integer
        results:
          type: array
          items:
            $ref: "#/components/schemas/SeoFinding"
        ownedPresence:
          type: boolean
        summary:
          type: string

    GeoModelResult:
      type: object
      required: [model, label, mentioned, accuracy, response, notes]
      properties:
        model:
          type: string
        label:
          type: string
        mentioned:
          type: boolean
        accuracy:
          type: string
          enum: [accurate, partial, none, incorrect]
        response:
          type: string
        notes:
          type: string

    GeoSource:
      type: object
      required: [title, url]
      properties:
        title:
          type: string
        url:
          type: string

    GeoFindings:
      type: object
      required: [models, summary]
      properties:
        models:
          type: array
          items:
            $ref: "#/components/schemas/GeoModelResult"
        summary:
          type: string
        sources:
          type: array
          items:
            $ref: "#/components/schemas/GeoSource"

    AuditResult:
      type: object
      required: [id, clientId, seoScore, geoScore, recommendations, status, createdAt]
      properties:
        id:
          type: integer
        clientId:
          type: integer
        seoScore:
          type: integer
        geoScore:
          type: integer
        seoFindings:
          oneOf:
            - $ref: "#/components/schemas/SeoFindings"
            - type: "null"
        geoFindings:
          oneOf:
            - $ref: "#/components/schemas/GeoFindings"
            - type: "null"
        recommendations:
          type: array
          items:
            type: string
        status:
          type: string
        createdAt:
          type: string

    AuditRefreshStatus:
      type: object
      required: [refreshing]
      properties:
        refreshing:
          type: boolean
          description: True if a background audit refresh is currently running for the user.

    AuditAutoRefreshResult:
      type: object
      required: [started]
      properties:
        started:
          type: boolean
          description: True if a background audit refresh was started by this request.
        reason:
          type: string
          description: Why a refresh was not started (e.g. no_client, no_prior_audit, fresh, in_flight).

    Competitor:
      type: object
      required: [name, description, positioning, differentiation]
      properties:
        name:
          type: string
        description:
          type: string
        positioning:
          type: string
        differentiation:
          type: string

    DossierSource:
      type: object
      required: [title, url]
      properties:
        title:
          type: string
        url:
          type: string

    BriefingDossier:
      type: object
      required: [id, clientId, footprintSummary, competitors, sources, generatedAt, createdAt]
      properties:
        id:
          type: integer
        clientId:
          type: integer
        footprintSummary:
          type: string
        competitors:
          type: array
          items:
            $ref: "#/components/schemas/Competitor"
        sources:
          type: array
          items:
            $ref: "#/components/schemas/DossierSource"
        generatedAt:
          type: string
        createdAt:
          type: string

    DossierGenerateInput:
      type: object
      properties:
        feedback:
          type: string
          description: Optional user revision notes to steer a regeneration.

    NarrativeTheme:
      type: object
      required: [title, description]
      properties:
        title:
          type: string
        description:
          type: string

    PlatformRecommendation:
      type: object
      required: [platform, reason, priority]
      properties:
        platform:
          type: string
        reason:
          type: string
        priority:
          type: string
          enum: [high, medium, low]

    IndustryAnswer:
      type: object
      required: [question, answer]
      properties:
        question:
          type: string
        answer:
          type: string

    PlatformPick:
      type: object
      required: [platform, reason]
      properties:
        platform:
          type: string
        reason:
          type: string

    OnlineChannel:
      type: object
      required: [recommendation, platforms]
      properties:
        recommendation:
          type: string
        platforms:
          type: array
          items:
            type: string

    WebsitePlan:
      type: object
      required: [recommendation, elements]
      properties:
        recommendation:
          type: string
        elements:
          type: array
          items:
            type: string

    OnlinePresence:
      type: object
      required: [primary, mirror, longForm, shortForm, website, newsletter]
      properties:
        primary:
          type: array
          items:
            $ref: "#/components/schemas/PlatformPick"
        mirror:
          type: array
          items:
            type: string
        longForm:
          $ref: "#/components/schemas/OnlineChannel"
        shortForm:
          $ref: "#/components/schemas/OnlineChannel"
        website:
          $ref: "#/components/schemas/WebsitePlan"
        newsletter:
          type: string

    OfflinePresence:
      type: object
      required: [intro, speaking, workshops, associations, teaching]
      properties:
        intro:
          type: string
        speaking:
          type: string
        workshops:
          type: string
        associations:
          type: string
        teaching:
          type: string

    PlatformStrategy:
      type: object
      required: [id, clientId, summary, online, offline, closing, createdAt, updatedAt]
      properties:
        id:
          type: integer
        clientId:
          type: integer
        summary:
          type: string
        online:
          $ref: "#/components/schemas/OnlinePresence"
        offline:
          $ref: "#/components/schemas/OfflinePresence"
        closing:
          type: string
        createdAt:
          type: string
        updatedAt:
          type: string

    IndustryPlayer:
      type: object
      required: [name, description, positioning]
      properties:
        name:
          type: string
        description:
          type: string
        positioning:
          type: string

    PlaybookMove:
      type: object
      required: [title, detail]
      properties:
        title:
          type: string
        detail:
          type: string

    IndustrySource:
      type: object
      required: [title, url]
      properties:
        title:
          type: string
        url:
          type: string

    IndustryOverview:
      type: object
      required:
        [
          id,
          clientId,
          industry,
          geographyFocus,
          landscapeContext,
          competitors,
          thoughtLeaders,
          playbook,
          sources,
          generatedAt,
          createdAt,
        ]
      properties:
        id:
          type: integer
        clientId:
          type: integer
        industry:
          type: string
        geographyFocus:
          type: string
        landscapeContext:
          type: string
        competitors:
          type: array
          items:
            $ref: "#/components/schemas/IndustryPlayer"
        thoughtLeaders:
          type: array
          items:
            $ref: "#/components/schemas/IndustryPlayer"
        playbook:
          type: array
          items:
            $ref: "#/components/schemas/PlaybookMove"
        sources:
          type: array
          items:
            $ref: "#/components/schemas/IndustrySource"
        generatedAt:
          type: string
        createdAt:
          type: string

    IndustryOverviewGenerateInput:
      type: object
      properties:
        feedback:
          type: string
          description: Optional user revision notes to steer a regeneration.

    PlatformCadence:
      type: object
      required: [platform, frequency, formats, focus]
      properties:
        platform:
          type: string
        frequency:
          type: string
        formats:
          type: array
          items:
            type: string
        focus:
          type: string

    ContentMixItem:
      type: object
      required: [type, description, whyForClient, exampleTopics, weight]
      properties:
        type:
          type: string
        description:
          type: string
        whyForClient:
          type: string
        exampleTopics:
          type: array
          items:
            type: string
        weight:
          type: string

    SignatureSeries:
      type: object
      required: [name, cadence, description]
      properties:
        name:
          type: string
        cadence:
          type: string
        description:
          type: string

    PostFormat:
      type: object
      required: [name, description]
      properties:
        name:
          type: string
        description:
          type: string

    ContentStrategy:
      type: object
      required:
        [
          id,
          clientId,
          summary,
          platformPlan,
          contentMix,
          signatureSeries,
          postFormats,
          repurposing,
          closing,
          createdAt,
          updatedAt,
        ]
      properties:
        id:
          type: integer
        clientId:
          type: integer
        summary:
          type: string
        platformPlan:
          type: array
          items:
            $ref: "#/components/schemas/PlatformCadence"
        contentMix:
          type: array
          items:
            $ref: "#/components/schemas/ContentMixItem"
        signatureSeries:
          type: array
          items:
            $ref: "#/components/schemas/SignatureSeries"
        postFormats:
          type: array
          items:
            $ref: "#/components/schemas/PostFormat"
        repurposing:
          type: string
        closing:
          type: string
        createdAt:
          type: string
        updatedAt:
          type: string

    NarrativeProfile:
      type: object
      required: [id, clientId, industryAnswers, coreNarrative, pointOfView, themes, recommendedPlatforms, contentHooks, createdAt, updatedAt]
      properties:
        id:
          type: integer
        clientId:
          type: integer
        industryAnswers:
          type: array
          items:
            $ref: "#/components/schemas/IndustryAnswer"
        coreNarrative:
          type: string
        pointOfView:
          type: string
        themes:
          type: array
          items:
            $ref: "#/components/schemas/NarrativeTheme"
        recommendedPlatforms:
          type: array
          items:
            $ref: "#/components/schemas/PlatformRecommendation"
        contentHooks:
          type: array
          items:
            type: string
        createdAt:
          type: string
        updatedAt:
          type: string

    NarrativeGenerateInput:
      type: object
      required: [answers]
      properties:
        answers:
          type: array
          items:
            $ref: "#/components/schemas/IndustryAnswer"
        feedback:
          type: string
          description: Optional user revision notes to steer a regeneration.

    PortraitGenerateInput:
      type: object
      properties:
        feedback:
          type: string
          description: Optional user revision notes to steer a regeneration.

    PlatformsGenerateInput:
      type: object
      properties:
        feedback:
          type: string
          description: Optional user revision notes to steer a regeneration.

    ContentStrategyGenerateInput:
      type: object
      properties:
        feedback:
          type: string
          description: Optional user revision notes to steer a regeneration.

    ContentPlanGenerateInput:
      type: object
      properties:
        startDate:
          type: string
          description: ISO date (YYYY-MM-DD) the plan should start from. Defaults to today.
        weeks:
          type: integer
          description: Number of weeks to plan (1-4). Defaults to 1.
        feedback:
          type: string
          description: Optional notes to steer the plan (e.g. emphasize a theme or platform).

    PlannedSlot:
      type: object
      required: [platform, title, format, contentType, brief, targetDate]
      properties:
        platform:
          type: string
          enum: [linkedin, twitter, instagram, blog, other]
        title:
          type: string
          description: A working title / hook for the post that will fill this slot.
        format:
          type: string
          description: The post format/shape (e.g. thread, carousel, long-form article).
        contentType:
          type: string
          description: Which content-mix bucket this slot serves (Educational, Analytical, Opinionated, Story, Community).
        brief:
          type: string
          description: A short brief the Ghostwriter will later expand into a full post.
        targetDate:
          type: string
          description: ISO timestamp for when this slot should publish.

    PlannedIdea:
      type: object
      required: [title, notes]
      properties:
        title:
          type: string
        notes:
          type: string
        platform:
          type: ["string", "null"]

    ContentPlanProposal:
      type: object
      required: [summary, startDate, weeks, slots, ideas]
      properties:
        summary:
          type: string
          description: 1-2 sentences framing the proposed week(s) of content.
        startDate:
          type: string
        weeks:
          type: integer
        slots:
          type: array
          items:
            $ref: "#/components/schemas/PlannedSlot"
        ideas:
          type: array
          items:
            $ref: "#/components/schemas/PlannedIdea"

    ContentPlanApplyInput:
      type: object
      required: [slots, ideas]
      properties:
        slots:
          type: array
          items:
            $ref: "#/components/schemas/PlannedSlot"
        ideas:
          type: array
          items:
            $ref: "#/components/schemas/PlannedIdea"

    ContentPlanApplyResult:
      type: object
      required: [posts, ideas]
      properties:
        posts:
          type: array
          items:
            $ref: "#/components/schemas/Post"
        ideas:
          type: array
          items:
            $ref: "#/components/schemas/Idea"

    ManagerRunInput:
      type: object
      required: [instruction]
      properties:
        instruction:
          type: string
          description: One high-level instruction for the Manager to decompose and delegate.

    ManagerProposalRef:
      type: object
      required: [title, rationale]
      properties:
        title:
          type: string
        rationale:
          type: string

    ManagerDraft:
      type: object
      required: [title, content, format]
      properties:
        title:
          type: string
        content:
          type: string
        format:
          type: string

    ManagerTaskOutput:
      type: object
      description: Agent-specific output for a task. Which fields are set depends on the task's agent.
      properties:
        footprintSummary:
          type: string
        competitorCount:
          type: integer
        assistantMessageId:
          type: integer
        reply:
          type: string
        proposals:
          type: array
          items:
            $ref: "#/components/schemas/ManagerProposalRef"
        planSummary:
          type: string
        slots:
          type: array
          items:
            $ref: "#/components/schemas/PlannedSlot"
        ideas:
          type: array
          items:
            $ref: "#/components/schemas/PlannedIdea"
        drafts:
          type: array
          items:
            $ref: "#/components/schemas/ManagerDraft"
        platform:
          type: string

    ManagerTask:
      type: object
      required: [id, agent, title, brief, status, resultSummary]
      properties:
        id:
          type: string
        agent:
          type: string
          enum: [investigator, strategist, planner, ghostwriter]
        title:
          type: string
        brief:
          type: string
        status:
          type: string
          enum: [pending, running, completed, failed, skipped]
        resultSummary:
          type: string
        error:
          type: ["string", "null"]
        output:
          oneOf:
            - $ref: "#/components/schemas/ManagerTaskOutput"
            - type: "null"

    ManagerRun:
      type: object
      required: [id, instruction, summary, status, tasks, createdAt, updatedAt]
      properties:
        id:
          type: integer
        instruction:
          type: string
        summary:
          type: string
        status:
          type: string
          enum: [completed, failed]
        tasks:
          type: array
          items:
            $ref: "#/components/schemas/ManagerTask"
        createdAt:
          type: string
        updatedAt:
          type: string

    AuditRunInput:
      type: object
      properties:
        feedback:
          type: string
          description: Optional user revision notes to steer a regeneration.

    NarrativeUpdateInput:
      type: object
      required: [coreNarrative, pointOfView, themes, recommendedPlatforms, contentHooks]
      properties:
        coreNarrative:
          type: string
        pointOfView:
          type: string
        themes:
          type: array
          items:
            $ref: "#/components/schemas/NarrativeTheme"
        recommendedPlatforms:
          type: array
          items:
            $ref: "#/components/schemas/PlatformRecommendation"
        contentHooks:
          type: array
          items:
            type: string

    PortraitSection:
      type: object
      required: [title, body]
      properties:
        title:
          type: string
        body:
          type: string

    ProfilePortrait:
      type: object
      required: [id, clientId, headline, summary, sections, stale, createdAt, updatedAt]
      properties:
        id:
          type: integer
        clientId:
          type: integer
        headline:
          type: string
        summary:
          type: string
        sections:
          type: array
          items:
            $ref: "#/components/schemas/PortraitSection"
        stale:
          type: boolean
        createdAt:
          type: string
        updatedAt:
          type: string

    Post:
      type: object
      required: [id, title, content, platform, status, createdAt, updatedAt]
      properties:
        id:
          type: integer
        title:
          type: string
        content:
          type: string
        platform:
          type: string
          enum: [linkedin, twitter, instagram, blog, other]
        status:
          type: string
          enum: [draft, scheduled, published]
        scheduledAt:
          type: ["string", "null"]
        tags:
          type: array
          items:
            type: string
        createdAt:
          type: string
        updatedAt:
          type: string

    PostInput:
      type: object
      required: [title, content, platform, status]
      properties:
        title:
          type: string
        content:
          type: string
        platform:
          type: string
          enum: [linkedin, twitter, instagram, blog, other]
        status:
          type: string
          enum: [draft, scheduled, published]
        scheduledAt:
          type: string
        tags:
          type: array
          items:
            type: string

    PostUpdate:
      type: object
      properties:
        title:
          type: string
        content:
          type: string
        platform:
          type: string
          enum: [linkedin, twitter, instagram, blog, other]
        status:
          type: string
          enum: [draft, scheduled, published]
        scheduledAt:
          type: string
        tags:
          type: array
          items:
            type: string

    DraftPostsInput:
      type: object
      required: [format, platform]
      properties:
        format:
          type: string
          enum: [post, hook, article]
          description: The kind of copy to draft — a full platform post, short scroll-stopping hooks, or a longer-form article.
        platform:
          type: string
          enum: [linkedin, twitter, instagram, blog, other]
        brief:
          type: string
          description: Optional freeform topic, angle, or instruction the draft should be about.
        ideaId:
          type: integer
          description: Optional saved idea to ground the draft in (its title and notes are used).
        theme:
          type: string
          description: Optional narrative theme or angle title to anchor the draft to.
        count:
          type: integer
          description: How many draft variants to produce (server-bounded).
        feedback:
          type: string
          description: Optional revision notes to steer a regeneration.

    DraftedPost:
      type: object
      required: [title, content, format]
      properties:
        title:
          type: string
        content:
          type: string
        format:
          type: string
          enum: [post, hook, article]

    DraftPostsResult:
      type: object
      required: [drafts]
      properties:
        drafts:
          type: array
          items:
            $ref: "#/components/schemas/DraftedPost"

    Idea:
      type: object
      required: [id, title, notes, createdAt]
      properties:
        id:
          type: integer
        title:
          type: string
        notes:
          type: string
        platform:
          type: ["string", "null"]
        createdAt:
          type: string

    IdeaInput:
      type: object
      required: [title]
      properties:
        title:
          type: string
        notes:
          type: string
        platform:
          type: string

    IdeaUpdate:
      type: object
      properties:
        title:
          type: string
        notes:
          type: string
        platform:
          type: string

    DashboardSummary:
      type: object
      required: [clientName, onboardingComplete, auditComplete, narrativeComplete, totalPosts, draftCount, scheduledCount, publishedCount, ideaCount, postsByPlatform, recentPosts]
      properties:
        clientName:
          type: ["string", "null"]
        onboardingComplete:
          type: boolean
        auditComplete:
          type: boolean
        narrativeComplete:
          type: boolean
        seoScore:
          type: ["integer", "null"]
        geoScore:
          type: ["integer", "null"]
        totalPosts:
          type: integer
        draftCount:
          type: integer
        scheduledCount:
          type: integer
        publishedCount:
          type: integer
        ideaCount:
          type: integer
        postsByPlatform:
          type: array
          items:
            $ref: "#/components/schemas/PlatformCount"
        recentPosts:
          type: array
          items:
            $ref: "#/components/schemas/Post"

    PlatformCount:
      type: object
      required: [platform, count]
      properties:
        platform:
          type: string
        count:
          type: integer

    AdminAccess:
      type: object
      required: [isAdmin]
      properties:
        isAdmin:
          type: boolean

    AdminUserSummary:
      type: object
      required: [clientId, userId, name, onboardingComplete, auditComplete, narrativeComplete, postCount, ideaCount, createdAt, updatedAt]
      properties:
        clientId:
          type: integer
        userId:
          type: string
        email:
          type: ["string", "null"]
        name:
          type: string
        onboardingComplete:
          type: boolean
        seoScore:
          type: ["integer", "null"]
        geoScore:
          type: ["integer", "null"]
        auditComplete:
          type: boolean
        narrativeComplete:
          type: boolean
        postCount:
          type: integer
        ideaCount:
          type: integer
        createdAt:
          type: string
        updatedAt:
          type: string

    AdminUserDetail:
      type: object
      required: [clientId, userId, profile, posts, ideas]
      properties:
        clientId:
          type: integer
        userId:
          type: string
        email:
          type: ["string", "null"]
        profile:
          $ref: "#/components/schemas/ClientProfile"
        audit:
          oneOf:
            - $ref: "#/components/schemas/AuditResult"
            - type: "null"
        narrative:
          oneOf:
            - $ref: "#/components/schemas/NarrativeProfile"
            - type: "null"
        posts:
          type: array
          items:
            $ref: "#/components/schemas/Post"
        ideas:
          type: array
          items:
            $ref: "#/components/schemas/Idea"

    PlatformsUpdateInput:
      type: object
      required: [summary, closing]
      properties:
        summary:
          type: string
        closing:
          type: string

    ContentStrategyUpdateInput:
      type: object
      required: [summary, repurposing, closing]
      properties:
        summary:
          type: string
        repurposing:
          type: string
        closing:
          type: string

    AssistantDiffItem:
      type: object
      required: [label, before, after]
      properties:
        label:
          type: string
        before:
          type: string
        after:
          type: string

    AssistantAction:
      type: object
      required: [id, kind, title, rationale, status, diff]
      properties:
        id:
          type: string
        kind:
          type: string
          enum:
            [
              update_profile,
              update_narrative,
              regenerate_narrative,
              update_content_strategy,
              update_platforms,
            ]
        title:
          type: string
        rationale:
          type: string
        status:
          type: string
          enum: [proposed, applied, rejected]
        rejectionComment:
          type: ["string", "null"]
        diff:
          type: array
          items:
            $ref: "#/components/schemas/AssistantDiffItem"
        payload:
          type: ["object", "null"]
          additionalProperties: true

    AssistantMessage:
      type: object
      required: [id, role, content, actions, seen, createdAt]
      properties:
        id:
          type: integer
        role:
          type: string
          enum: [user, assistant]
        content:
          type: string
        actions:
          type: array
          items:
            $ref: "#/components/schemas/AssistantAction"
        seen:
          type: boolean
        createdAt:
          type: string

    AssistantUnread:
      type: object
      required: [count]
      properties:
        count:
          type: integer

    AssistantSendInput:
      type: object
      required: [content]
      properties:
        content:
          type: string

    AssistantReply:
      type: object
      required: [userMessage, assistantMessage]
      properties:
        userMessage:
          $ref: "#/components/schemas/AssistantMessage"
        assistantMessage:
          $ref: "#/components/schemas/AssistantMessage"

    AssistantRejectInput:
      type: object
      properties:
        comment:
          type: string

    AssistantActionResult:
      type: object
      required: [action]
      properties:
        action:
          $ref: "#/components/schemas/AssistantAction"
        assistantMessage:
          oneOf:
            - $ref: "#/components/schemas/AssistantMessage"
            - type: "null"

    AssistantBatchInput:
      type: object
      required: [actionIds]
      properties:
        actionIds:
          type: array
          items:
            type: string

    AssistantBatchResult:
      type: object
      required: [actions]
      properties:
        actions:
          type: array
          items:
            $ref: "#/components/schemas/AssistantAction"

    ScheduleBatchInput:
      type: object
      required: [postIds, startDate]
      properties:
        postIds:
          type: array
          items:
            type: integer
          description: Post IDs to schedule, in the order they should be laid out.
        startDate:
          type: string
          description: ISO date for the first post (YYYY-MM-DD).
        intervalDays:
          type: integer
          description: Days between consecutive posts. Defaults to 1.
        time:
          type: string
          description: Time of day in HH:MM (24h). Defaults to 09:00.
